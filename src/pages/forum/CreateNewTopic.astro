---
import '@/styles/global.css'
import Layout from "@/layout/Layout.astro";
import NewTopic from '@/components/NewTopic.jsx'
import { getSession } from 'auth-astro/server';
import getUserUuid from '@/services/getUserUuid';
const session = await getSession(Astro.request)
console.log(session)

const userUUID = await getUserUuid({id: session?.user?.id, provider: 'twitch'})
console.log(userUUID)
---


<Layout title="New Topic">

    <NewTopic user_id={userUUID} client:load/>
</Layout>